# Templates

Use this folder for reusable starter structures that help create new examples consistently.

Template folders should be copied into `examples/<year>/` or `labs/<year>/` and then renamed for the specific topic.

