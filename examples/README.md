# Examples

This folder contains code examples used during class. Examples are organized by year so the repository can grow across future offerings without overwriting earlier material.

Use the following pattern for new examples:

```text
examples/<year>/<example-name>/
```

Each example folder should include a `README.md` with the topic, run instructions, and any notes students need before experimenting.

